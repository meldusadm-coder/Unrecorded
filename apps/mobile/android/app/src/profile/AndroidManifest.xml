<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Debug/profile overlay: Flutter tooling (hot reload). Release builds also
         require INTERNET via AdMob, UMP consent, and Play Billing (merged from SDKs). -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
