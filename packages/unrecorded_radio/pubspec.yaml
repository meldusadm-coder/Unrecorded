name: unrecorded_radio
description: Radio scanner abstraction for Unrecorded — fake and BLE implementations.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.22.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.35.0
  unrecorded_core:
    path: ../unrecorded_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
